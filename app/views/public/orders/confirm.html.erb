<h1>注文情報確認</h1>

<table>
  <thead>
    <th>商品名</th>
    <th>単価(税込)</th>
    <th>数量</th>
    <th>小計</th>
  </thead>
  <tbody>
    <% @cart_items.each do |cart_item| %>
      <tr>
        <td><%= cart_item.item.name %></td>
        <td><%= cart_item.item.with_tax_price %></td>
        <td><%= cart_item.quantity %></td>
        <td><%= cart_item.subtotal %></td>
      </tr>
    <% end %>
  </tbody>
</table>

</table>


<h2>支払い方法</h2>
<p><%= @order.payment_method == 'credit_card' ? 'クレジットカード' : '銀行振込' %></p>

<h3>お届け先</h3>
<div>
  <p>氏名: <%= current_customer.full_name %></p>
  <p>郵便番号: <%= current_customer.postal_code %></p>
  <p>住所: <%= current_customer.address %></p>
</div>

<%= link_to "注文を確定する",  public_orders_complete_path, class: "btn btn-primary" %>
